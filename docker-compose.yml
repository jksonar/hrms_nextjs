version: '3.9'
version: '3.9'
services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env.dev
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app.db:/app/app.db
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
